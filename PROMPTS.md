<!--
AUTO-GENERATED BY prjctl - DO NOT EDIT DIRECTLY.
To modify: prjctl edit rust-local-rag PROMPTS
Generated: 2026-01-12T16:44:00+00:00
PRJCTL-META: {"body_sha256":"sha256:696107955644da038076dc0ed7819a8180273b5cc934e417bbb1a7cc9da05980","deps":[{"name":"_base/PROMPTS.md.j2","path":"/Users/ryanpappal/03_CODE/MCP_MASTER/context-assembler/sources/_base/PROMPTS.md.j2","role":"template"},{"name":"PROMPTS.local.md","path":"/Users/ryanpappal/03_CODE/MCP_MASTER/context-assembler/sources/overlays/rust-local-rag/PROMPTS.local.md","role":"local_additions"},{"name":"base.jules.daily.docs.doc_alignment_audit_one","path":"CODE_VAULT/configs/prompts/_base/jules/daily/doc_alignment_audit_one.prompt.md","role":"prompt"},{"name":"rust_local_rag.jules.daily.hygiene.cargo_quality_gate","path":"CODE_VAULT/configs/prompts/rust-local-rag/jules/daily/cargo_quality_gate.prompt.md","role":"prompt"},{"name":"rust_local_rag.jules.daily.quality.chunking_edge_case_tests","path":"CODE_VAULT/configs/prompts/rust-local-rag/jules/daily/chunking_edge_case_tests.prompt.md","role":"prompt"},{"name":"rust_local_rag.jules.daily.observability.error_tracing_instrumentation_one","path":"CODE_VAULT/configs/prompts/rust-local-rag/jules/daily/error_tracing_instrumentation_one.prompt.md","role":"prompt"},{"name":"rust_local_rag.jules.daily.reliability.unwrap_expect_removal_one","path":"CODE_VAULT/configs/prompts/rust-local-rag/jules/daily/unwrap_expect_removal_one.prompt.md","role":"prompt"},{"name":"base.jules.weekly.docs.todo_fixme_snapshot","path":"CODE_VAULT/configs/prompts/_base/jules/weekly/todo_fixme_snapshot.prompt.md","role":"prompt"},{"name":"base.jules.weekly.maint.todo_burndown_one","path":"CODE_VAULT/configs/prompts/_base/jules/weekly/todo_burndown_one.prompt.md","role":"prompt"},{"name":"base.jules.weekly.t_task.impact_drill","path":"CODE_VAULT/configs/prompts/_base/jules/weekly/t_task_impact_drill.prompt.md","role":"prompt"},{"name":"rust_local_rag.jules.weekly.deps.cargo_update_patch","path":"CODE_VAULT/configs/prompts/rust-local-rag/jules/weekly/cargo_update_patch.prompt.md","role":"prompt"},{"name":"rust_local_rag.jules.weekly.refactor.grand_rounds_refactor","path":"CODE_VAULT/configs/prompts/rust-local-rag/jules/weekly/grand_rounds_refactor.prompt.md","role":"prompt"},{"name":"rust_local_rag.jules.weekly.t_task.refactor.unwrap_clone_hardening","path":"CODE_VAULT/configs/prompts/rust-local-rag/jules/weekly/t_task_unwrap_clone_hardening.prompt.md","role":"prompt"}],"generated_at":"2026-01-12T16:44:00+00:00","imports":[],"meta_version":1,"prjctl_version":"0.1.0","project":"rust-local-rag","prompt_catalog_hash":"sha256:50fba03a6e51f788e6ce32eca2cdb766a59964ef31ed3b461703135233c09fbe","source_hash":"sha256:d812c3c6053c7483dd8f74d2f81fe485e25a380f5b363fbaa0be4437bec4a4c0","ssot_root":"/Users/ryanpappal/My Drive/Obsidian Vaults/CCC_2025-10-24/CODE_VAULT/configs","target":"PROMPTS.md","top_template":"_base/PROMPTS.md.j2"}
-->

# Prompt Library — rust-local-rag

These prompts are managed by `prjctl`/`mcpmaster` from SSOT (`CODE_VAULT/configs/prompts`).
Do not edit directly. Prefer `mcpmaster project prompts show <id>`.

## jules — daily
### base.jules.daily.docs.doc_alignment_audit_one
- Title: Daily docs/code alignment audit: fix ONE document precisely
- Tags: audit, cloud, docs, t_task
- Risk: low
- Owner: ryan
- Status: active
- DoD: Selects exactly one high-value doc (README/docs/AGENTS/CLAUDE/etc); Verifies claims against the live codebase (no guessing); Fixes stale or missing info in that doc only (docs-only PR); PR includes: claims checked + what was corrected + commands used

```text
[STRICT MODE — Unattended Cloud Run]

Cloud VM. No LAN/NAS. No secrets. No MCP tools. Do not change application code in this task.

This is a **T-task**:
- **Top bar:** scan docs quickly for likely stale claims.
- **Stalk:** deeply verify and correct ONE chosen document.

Phase 1 — Select ONE document:
1) Scan repo root and `docs/` for “core documents”.
2) Pick exactly one of: `AGENTS.md`, `CLAUDE.md`, `README.md`, `CONTRIBUTING.md`, or the most critical doc in `docs/`.
3) State why this doc is the best target today (high impact / likely stale / referenced often).

Phase 2 — Don’t trust, verify:
1) Extract key technical claims (commands, file paths, architecture, APIs).
2) Verify each claim with concrete evidence:
   - `ls`, `rg`, `cat`, `python -m`, `deno task`, `cargo` (read-only checks preferred)
3) Make a list of:
   - “Stale Information” (wrong now)
   - “Missing Information” (new behavior not documented)

Phase 3 — Fix docs (one cohesive PR):
- Update ONLY the chosen doc.
- Prefer concise, factual writing; remove fluff.
- Ensure examples/commands are correct.

If blocked:
- If you can’t verify a claim from the repo itself, do not guess; mark it as “needs manual verification”.

```

### rust_local_rag.jules.daily.hygiene.cargo_quality_gate
- Title: Daily Rust quality gate: fmt --check + clippy -D warnings + test
- Tags: clippy, cloud, hygiene, rust, tests
- Risk: low
- Owner: ryan
- Status: active
- DoD: cargo fmt --check passes (or formatting is applied); cargo clippy -- -D warnings passes; cargo test passes; If initially green: includes one small improvement (lint cleanup / test / error handling); PR includes commands run + results

```text
[STRICT MODE — Unattended Cloud Run]

You are in a cloud VM. No LAN. No secrets. Do not rely on local daemons (e.g., Ollama).

Task: Keep rust-local-rag’s Rust checks green with minimal changes.

Steps:
1) Format check:
   - `cargo fmt -- --check`
   - If it fails, run `cargo fmt` and re-check.
2) Lint:
   - `cargo clippy --all-targets --all-features -- -D warnings`
3) Tests:
   - `cargo test`
4) If everything is already green:
   - Make one small, high-confidence improvement:
     - remove a clippy warning the strict CI would catch,
     - replace one `unwrap()` in a non-test path with proper error propagation,
     - add one unit test for a real edge case.

If blocked:
- If builds require missing system libraries, do not attempt to install OS packages; document the missing dependency and stop.
```

### rust_local_rag.jules.daily.quality.chunking_edge_case_tests
- Title: Daily quality: harden ONE chunking edge case (tests + fix if needed)
- Tags: chunking, cloud, quality, rust, t_task, tests
- Risk: low
- Owner: ryan
- Status: active
- DoD: Finds one chunking edge case risk and adds unit test(s) for it; If bug found: fixes it with minimal blast radius; cargo fmt --check + clippy -D warnings + test pass; No Ollama/local daemon required

```text
[STRICT MODE — Unattended Cloud Run]

Cloud VM. No LAN. No secrets. Do not run Ollama or any local services. Follow repo `AGENTS.md`.

This is a **T-task**:
- **Top bar:** scan chunking/text-splitting code for likely panic/logic edge cases.
- **Stalk:** add deep tests and fix exactly one issue if found.

Hard constraints:
- Choose exactly ONE stalk target inside the chunking subsystem.
- Max 4 files changed total.

Top bar scan:
1) Locate chunking implementation (likely in `crates/*/src/*chunk*` or similar).
2) Identify one likely edge case:
   - empty input
   - very small input
   - multi-byte Unicode boundaries
   - no separators / very long tokens

Stalk (execute):
1) Add 3–5 unit tests covering the chosen edge case (use existing test patterns).
2) If tests reveal a bug or panic risk, implement the smallest fix.

Verification (must run):
1) `cargo fmt -- --check` (if fails: `cargo fmt`, then re-check)
2) `cargo clippy --all-targets --all-features -- -D warnings`
3) `cargo test`

```

### rust_local_rag.jules.daily.observability.error_tracing_instrumentation_one
- Title: Daily observability: instrument ONE MCP handler for better tracing (no new deps)
- Tags: cloud, mcp, observability, rust, t_task, tracing
- Risk: medium
- Owner: ryan
- Status: active
- DoD: Identifies 3 candidate handlers for better tracing and chooses one stalk target; Adds/adjusts tracing so failures preserve context (without logging secrets); Updates/creates a unit test if feasible (otherwise documents verification reasoning); cargo fmt --check + clippy -D warnings + test pass

```text
[STRICT MODE — Unattended Cloud Run]

Cloud VM. No LAN. No secrets. Do not run Ollama. Follow repo `AGENTS.md`.

This is a **T-task**:
- **Top bar:** scan the MCP server surface for missing tracing context.
- **Stalk:** instrument ONE handler deeply to improve remote debugging.

Hard constraints:
- Exactly ONE stalk target.
- Max 6 files changed total.
- No new dependencies.

Top bar scan:
1) Identify where MCP tool handlers are implemented.
2) Pick 3 candidate handlers where tracing would help:
   - tool call entry/exit
   - error mapping into MCP response
   - long-running operations with durations

Stalk (implement):
1) Add/adjust `tracing` instrumentation to capture:
   - handler name / tool name
   - duration
   - key non-sensitive params (redact any token/path-like secrets)
2) Ensure errors are logged with context before returning a structured error to the client.

Verification:
1) `cargo fmt -- --check` (if fails: `cargo fmt`, then re-check)
2) `cargo clippy --all-targets --all-features -- -D warnings`
3) `cargo test`

Stop conditions:
- If the only way to verify is running the server end-to-end, keep changes minimal and rely on unit tests + compile-time checks; do not start services.

```

### rust_local_rag.jules.daily.reliability.unwrap_expect_removal_one
- Title: Daily reliability: remove ONE unwrap/expect in runtime path (add test if possible)
- Tags: cloud, errors, reliability, rust, t_task
- Risk: medium
- Owner: ryan
- Status: active
- DoD: Finds one unwrap/expect in a runtime (non-test) path and removes it safely; Adds/updates a unit test for the failure mode when feasible; cargo fmt --check + clippy -D warnings + test pass; Change is bounded (<= 3 files ideally) and behavior is explicit

```text
[STRICT MODE — Unattended Cloud Run]

Cloud VM. No LAN. No secrets. Do not run Ollama/local services. Follow repo `AGENTS.md`.

This is a **T-task**:
- **Top bar:** scan for `unwrap()`/`expect()` in non-test code that could panic in production.
- **Stalk:** replace exactly ONE with explicit error propagation/mapping and tests when possible.

Hard constraints:
- Exactly ONE stalk target unwrap/expect site.
- Max 4 files changed total.
- No new dependencies.

Top bar scan:
1) Find candidates: `rg -n \"\\.(unwrap|expect)\\(\" src crates -S`
2) Filter to runtime paths (exclude tests, examples).
3) Pick ONE candidate with highest likelihood of panic + easiest to test.

Stalk (implement):
1) Replace unwrap/expect with:
   - `?` propagation + context, OR
   - explicit match that returns a structured error
2) Add/upgrade unit tests for the failure mode if feasible.

Verification:
1) `cargo fmt -- --check` (if fails: `cargo fmt`, then re-check)
2) `cargo clippy --all-targets --all-features -- -D warnings`
3) `cargo test`

```


## jules — weekly
### base.jules.weekly.docs.todo_fixme_snapshot
- Title: Weekly TODO/FIXME snapshot + prioritized fix list (docs-only)
- Tags: debt, docs, hygiene, scan, triage
- Risk: low
- Owner: ryan
- Status: active
- DoD: Creates/updates analysis/TECHNICAL_DEBT_SNAPSHOT.md; Does not change non-doc source files; Includes a prioritized 'next 5 fixes' list with estimated effort; PR description includes commands run + results

```text
[STRICT MODE — Unattended Cloud Run]

Environment assumptions:
- You are running in a cloud VM.
- NO access to the operator’s home LAN/NAS.
- No secrets. Do not request or create `.env` files.
- Follow repo `AGENTS.md` for any repo-specific safety rules.

Task: Produce a weekly “technical debt snapshot” *and* a prioritized short list of fixes.

Scope constraints (non-negotiable):
- Docs-only change: you may ONLY modify `analysis/TECHNICAL_DEBT_SNAPSHOT.md` (create if missing).
- Do not change any code, configs, lockfiles, or tests in this task.
- Exclude typical vendor/build dirs: `node_modules/`, `.venv/`, `target/`, `.git/`, `.archive/`, `dist/`, `build/`.

Required output format (in `analysis/TECHNICAL_DEBT_SNAPSHOT.md`):
1) Header:
   - Timestamp (UTC)
   - Repo name
2) Summary table:
   - Counts by marker type: TODO / FIXME / HACK / XXX / NOTE
   - Top 10 files by count
3) “High-risk markers” section (prioritize surfacing):
   - security/auth
   - data loss/deletion/migration
   - lookahead/PIT/leakage/survivorship
   - correctness/determinism/flaky tests
4) Findings list (cap to 200 entries total; deterministic order):
   - file path + line number + marker line (trim to 160 chars)
5) “Next 5 fixes” section:
   - For each: prompt-sized fix description, estimated effort (S/M/L), and suggested verification command(s)

If blocked:
- Use whichever is available (`rg` preferred, else `grep`) and keep ordering deterministic.
- If repo is huge, state truncation explicitly and still include the “Next 5 fixes” list.

```

### base.jules.weekly.maint.todo_burndown_one
- Title: Weekly TODO/FIXME burndown: implement ONE real fix + test
- Tags: cloud, debt, implementation, maint, tests
- Risk: medium
- Owner: ryan
- Status: active
- DoD: Selects exactly one concrete TODO/FIXME/HACK/XXX item and resolves it; Adds/updates at least one regression test (or a rationale why not possible); Runs the project’s primary verification command(s) and they pass; PR includes: what changed, why, and commands run + results

```text
[STRICT MODE — Unattended Cloud Run]

You are running in a cloud VM. NO home LAN/NAS. No secrets. Follow repo `AGENTS.md`.

This is NOT a report-only task. This must produce a meaningful PR.

This is a **T-task**:
- **Top bar:** scan broadly across TODO/FIXME items to choose the best leverage target.
- **Stalk:** go deep in ONE area to fully resolve it with tests, while keeping blast radius small.

Hard constraints:
- Fix exactly ONE TODO/FIXME/HACK/XXX item (not more).
- The fix must be cloud-safe: no LAN services, no private endpoints, no secrets.
- Keep scope bounded: ≤ 5 files changed total.

Plan (must be explicit and brief):
0) Top bar scan:
   - briefly summarize the top 3 candidate TODO/FIXME items you found (file + why it matters)
1) Identify the chosen TODO/FIXME (your stalk target):
   - file path + line number
   - why it matters (correctness / robustness / maintainability)
2) Proposed change:
   - exact functions/modules to touch
3) Verification commands:
   - the exact commands you will run at the end
4) Stop conditions:
   - when you will bail instead of guessing (e.g., requires secrets, requires LAN, unclear intent)

Execute:
1) Implement the fix.
2) Remove the TODO/FIXME comment (or rewrite it into a precise comment if the TODO cannot be eliminated safely).
3) Add at least one regression test or invariant-checking test when feasible.
4) Run verification commands and ensure green.

If blocked:
- If the TODO requires secrets/LAN or intent is unclear, do not guess. Abort implementation and instead update a docs-only location with a crisp “blocked” note (only if repo has an obvious place); otherwise exit with logs.
```

### base.jules.weekly.t_task.impact_drill
- Title: Weekly T-Task impact drill: scan broadly, then improve one module deeply
- Tags: cloud, impact, maint, t_task, tests
- Risk: medium
- Owner: ryan
- Status: active
- DoD: Completes a broad scan (Top bar) and records findings in PR description; Selects exactly one target area (Stalk) using explicit rubric; Implements a deep improvement in that area (not cosmetic) within bounds; Adds/updates tests (or states why not feasible) and passes verification commands

```text
[STRICT MODE — Unattended Cloud Run]

You are running in a cloud VM. NO home LAN/NAS access. No secrets. Do not use MCP tools or assume any local daemons exist.
Follow repo `AGENTS.md` for project-specific rules and preferred commands.

This is a **T-task**:
- **Top bar (breadth):** a thin, repo-wide scan for one category of leverage.
- **Stalk (depth):** a deep, modular improvement in ONE chosen area to maximize impact while minimizing blast radius.

Non-negotiable constraints:
- Choose ONE stalk target only.
- Cap blast radius: ≤ 10 files changed total.
- Must be cloud-safe (no secrets, no LAN dependencies).

Top bar (required scan):
1) Determine the repo’s standard verification commands:
   - Look for `Makefile`, `justfile`, `pyproject.toml`, `deno.json`, `Cargo.toml`, `package.json`, `scripts/`.
   - Identify the “fast” check command (lint/type/test) that runs offline.
2) Scan for leverage candidates (pick one category, then scan broadly):
   - repeated TODO/FIXME clusters in core code
   - flaky or frequently failing tests
   - overly complex module (very long function, duplicated logic, confusing error handling)
   - brittle parsing/validation (weak input validation, unclear error messages)
3) Produce a short candidate list (3–5 items) with:
   - file path(s)
   - why it matters
   - why it is cloud-safe

Selection rubric (choose exactly ONE stalk):
- Highest leverage + testable + cloud-safe + bounded scope.

Stalk (deep implementation):
1) Write a brief plan:
   - which files will change
   - what tests you will add/modify
   - the exact verification command chain you will run at the end
2) Implement the improvement:
   - be bold inside the chosen module (refactor for clarity/robustness/perf)
   - but do NOT sprawl into unrelated subsystems
3) Add/extend tests to prove the improvement and prevent regressions.
4) Run verification and ensure green.

If blocked:
- If your chosen stalk requires secrets/LAN, abandon it and pick a different stalk from your candidate list.

```

### rust_local_rag.jules.weekly.deps.cargo_update_patch
- Title: Weekly Rust dependency update (Cargo.lock) + verification
- Tags: cloud, deps, hygiene, rust
- Risk: medium
- Owner: ryan
- Status: active
- DoD: cargo update completes and updates Cargo.lock; cargo fmt --check + cargo clippy -D warnings + cargo test pass; Any required compatibility fixes included (bounded) OR breaking updates reverted; PR includes brief bump summary + commands run + results

```text
[STRICT MODE — Unattended Cloud Run]

You are in a cloud VM. No LAN. No secrets.

Task: Update Rust dependencies conservatively (Cargo.lock only) and verify.

Hard constraints:
- Do not manually edit `Cargo.toml` in this task; update `Cargo.lock` via `cargo update`.
- If the update introduces failures, revert and stop rather than forcing risky changes.

Steps:
1) `cargo update`
2) Verify:
   - `cargo fmt -- --check`
   - `cargo clippy --all-targets --all-features -- -D warnings`
   - `cargo test`
3) If failures occur, revert `Cargo.lock` and open a PR explaining which crate update broke and why.
```

### rust_local_rag.jules.weekly.refactor.grand_rounds_refactor
- Title: Weekly Grand Rounds: meaningful Rust refactor (less unwrap/clone) + tests
- Tags: clippy, cloud, refactor, rust, tests
- Risk: medium
- Owner: ryan
- Status: active
- DoD: Refactors one subsystem/module for correctness/robustness/performance; Reduces at least one real risk pattern (unwrap/expect in non-test paths, excessive clones, weak error handling); cargo fmt --check + cargo clippy -D warnings + cargo test pass; PR explains: what changed, why it matters, and how it was verified

```text
[STRICT MODE — Unattended Cloud Run]

Cloud VM. No LAN/NAS. No secrets. Do not rely on local daemons (e.g., Ollama). Follow repo `AGENTS.md`.

Goal: A material, review-worthy refactor PR (not cosmetic).

Scope constraints:
- One subsystem/module only (≤ 6 files).
- No feature sprawl: focus on robustness/perf/correctness in one place.

Plan (required):
1) Target module/subsystem and the exact pain points (e.g., unwraps, clones, error handling).
2) What you will change and how you will prove correctness (tests).
3) Command chain you will run at the end.

Execution ideas (pick 1–2 max, but keep within one module):
- Replace `unwrap()/expect()` in non-test code paths with proper `Result` propagation + good error messages.
- Reduce unnecessary cloning by using references/borrowing and targeted ownership changes.
- Add missing unit tests for edge cases around parsing, IO boundaries (mocked), or ranking logic.

Verification (must run):
1) `cargo fmt -- --check` (if fails: `cargo fmt`, then re-check)
2) `cargo clippy --all-targets --all-features -- -D warnings`
3) `cargo test`

If blocked:
- If the change balloons beyond the module boundary, stop and reduce scope to the smallest valuable subset.

```

### rust_local_rag.jules.weekly.t_task.refactor.unwrap_clone_hardening
- Title: Weekly T-Task: remove unwrap/expect + reduce clones in one module (with tests)
- Tags: clippy, cloud, refactor, rust, t_task, tests
- Risk: medium
- Owner: ryan
- Status: active
- DoD: Completes a broad scan for unwrap/expect/clone density and summarizes candidates in PR description; Selects exactly one module to harden (≤ 6 files) using explicit rubric; Removes unwrap/expect in non-test paths in that module and improves error propagation/messages; Reduces unnecessary clones where feasible without breaking APIs; Verification passes: cargo fmt --check + clippy -D warnings + cargo test

```text
[STRICT MODE — Unattended Cloud Run]

Cloud VM. No LAN/NAS. No secrets. Do not rely on local daemons (e.g., Ollama). Follow repo `AGENTS.md`.

This is a **T-task**:
- **Top bar:** scan broadly for panic/perf risk patterns.
- **Stalk:** harden ONE module deeply to materially improve robustness and maintainability.

Hard constraints:
- Choose exactly ONE stalk module.
- Max 6 files changed total (including tests).
- Avoid feature sprawl; focus on error handling + robustness.

Top bar (broad scan — do not edit yet):
1) Scan for panic risks and perf smells:
   - `unwrap()` / `expect(...)` in non-test code paths
   - high clone density (`.clone()`) where borrowing could work
2) Gather candidates:
   - Use repo search tools to find unwrap/expect/clone usage.
   - Optionally run clippy (non-fatal) to see baseline warnings.
3) Produce a candidate list (3–5) with:
   - module path(s)
   - what patterns exist (unwrap/expect/clone)
   - why it’s high leverage

Selection rubric (choose ONE stalk module):
- Core logic module + high unwrap/expect density + testable with unit tests + bounded scope.

Stalk (deep implementation):
1) Plan (required):
   - chosen module and why
   - files to change (≤ 6)
   - what error-handling approach you’ll use (existing crate patterns)
   - what test(s) you’ll add
2) Remove panics:
   - Replace unwrap/expect in non-test code with `Result` propagation (`?`) and meaningful errors.
   - Preserve context (what operation failed) in error messages.
3) Reduce clones:
   - Replace obvious unnecessary clones with borrowing where possible.
   - Keep the change bounded; don’t rewrite whole APIs.
4) Tests:
   - Add a unit test that exercises an error path that previously would have panicked or been untested.

Verification (must run, in order):
1) `cargo fmt -- --check` (if fails: `cargo fmt`, then re-check)
2) `cargo clippy --all-targets --all-features -- -D warnings`
3) `cargo test`

If blocked:
- If this module’s changes explode into widespread signature changes, reduce scope to the smallest valuable subset and stop at ≤ 6 files.

```




