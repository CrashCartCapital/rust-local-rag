<!--
AUTO-GENERATED BY prjctl - DO NOT EDIT DIRECTLY.
To modify: prjctl edit rust-local-rag PROMPTS
Generated: 2026-01-11T23:09:18+00:00
PRJCTL-META: {"body_sha256":"sha256:c62ed9285cef0e8530f4d4ffaa49e39e9d6ccdc5bdcb5e3a333e477a21f8e272","deps":[{"name":"_base/PROMPTS.md.j2","path":"/Users/ryanpappal/03_CODE/MCP_MASTER/context-assembler/sources/_base/PROMPTS.md.j2","role":"template"},{"name":"PROMPTS.local.md","path":"/Users/ryanpappal/03_CODE/MCP_MASTER/context-assembler/sources/overlays/rust-local-rag/PROMPTS.local.md","role":"local_additions"},{"name":"rust_local_rag.jules.daily.hygiene.cargo_quality_gate","path":"CODE_VAULT/configs/prompts/rust-local-rag/jules/daily/cargo_quality_gate.prompt.md","role":"prompt"},{"name":"base.jules.weekly.docs.todo_fixme_snapshot","path":"CODE_VAULT/configs/prompts/_base/jules/weekly/todo_fixme_snapshot.prompt.md","role":"prompt"},{"name":"rust_local_rag.jules.weekly.deps.cargo_update_patch","path":"CODE_VAULT/configs/prompts/rust-local-rag/jules/weekly/cargo_update_patch.prompt.md","role":"prompt"}],"generated_at":"2026-01-11T23:09:18+00:00","imports":[],"meta_version":1,"prjctl_version":"0.1.0","project":"rust-local-rag","prompt_catalog_hash":"sha256:54b71ab8961bcba7d3a13b27e9d337b9efbf945da080126fdbec55545650685b","source_hash":"sha256:e47feb8421172745d3487ddd3085680d6ff44e7ca7ac0f35d126e00bd1b6cc8f","ssot_root":"/Users/ryanpappal/My Drive/Obsidian Vaults/CCC_2025-10-24/CODE_VAULT/configs","target":"PROMPTS.md","top_template":"_base/PROMPTS.md.j2"}
-->

# Prompt Library — rust-local-rag

These prompts are managed by `prjctl`/`mcpmaster` from SSOT (`CODE_VAULT/configs/prompts`).
Do not edit directly. Prefer `mcpmaster project prompts show <id>`.

## jules — daily
### rust_local_rag.jules.daily.hygiene.cargo_quality_gate
- Title: Daily Rust quality gate: fmt --check + clippy -D warnings + test
- Tags: clippy, cloud, hygiene, rust, tests
- Risk: low
- Owner: ryan
- Status: active
- DoD: cargo fmt --check passes (or formatting is applied); cargo clippy -- -D warnings passes; cargo test passes; PR includes commands run + results

```text
[STRICT MODE — Unattended Cloud Run]

You are in a cloud VM. No LAN. No secrets. Do not rely on local daemons (e.g., Ollama).

Task: Keep rust-local-rag’s Rust checks green with minimal changes.

Steps:
1) Format check:
   - `cargo fmt -- --check`
   - If it fails, run `cargo fmt` and re-check.
2) Lint:
   - `cargo clippy --all-targets --all-features -- -D warnings`
3) Tests:
   - `cargo test`
4) Fix minimal issues only; avoid large refactors.

If blocked:
- If builds require missing system libraries, do not attempt to install OS packages; document the missing dependency and stop.

```


## jules — weekly
### base.jules.weekly.docs.todo_fixme_snapshot
- Title: Weekly TODO/FIXME snapshot (docs-only)
- Tags: debt, docs, hygiene, scan
- Risk: low
- Owner: ryan
- Status: active
- DoD: Creates/updates analysis/TECHNICAL_DEBT_SNAPSHOT.md; Does not change non-doc source files; PR description includes commands run + results

```text
[STRICT MODE — Unattended Cloud Run]

Environment assumptions:
- You are running in a cloud VM.
- You have internet for public package registries / git fetch, but NO access to the operator’s home LAN/NAS.
- Do NOT attempt to use MCP tools, local daemons, or private endpoints.
- No secrets are available. Do not request or create `.env` files.

Task: Create a weekly “technical debt snapshot” by scanning the repo for TODO/FIXME/HACK/XXX markers.

Scope constraints (non-negotiable):
- Docs-only change: you may ONLY modify `analysis/TECHNICAL_DEBT_SNAPSHOT.md` (create if missing).
- Do not change any code, configs, lockfiles, or tests in this task.
- Exclude typical vendor/build dirs (`node_modules/`, `.venv/`, `target/`, `.git/`, `.archive/`).

Steps:
1) Scan for markers: `TODO`, `FIXME`, `HACK`, `XXX`, `NOTE` in source-like files.
2) Summarize into `analysis/TECHNICAL_DEBT_SNAPSHOT.md` with:
   - Timestamp (UTC)
   - Counts by marker type
   - Top 10 files by count
   - A “high-risk markers” section: lines mentioning security, auth, PIT/lookahead, data loss, deletion, migrations
   - For each item: file path + line number + the marker line (trimmed)
3) Open a PR with that single-file change.

If blocked:
- If scan tools differ, use whatever is available (`rg`, `grep`) but keep output deterministic.
- If repo is huge, cap the report to top 200 total findings and note truncation explicitly.

```

### rust_local_rag.jules.weekly.deps.cargo_update_patch
- Title: Weekly Rust dependency update (Cargo.lock) + verification
- Tags: cloud, deps, hygiene, rust
- Risk: medium
- Owner: ryan
- Status: active
- DoD: cargo update completes and updates Cargo.lock; cargo fmt --check passes; cargo clippy -D warnings passes; cargo test passes; PR includes brief bump summary

```text
[STRICT MODE — Unattended Cloud Run]

You are in a cloud VM. No LAN. No secrets.

Task: Update Rust dependencies conservatively (Cargo.lock only) and verify.

Hard constraints:
- Do not manually edit `Cargo.toml` in this task; update `Cargo.lock` via `cargo update`.
- If the update introduces failures, revert and stop rather than forcing risky changes.

Steps:
1) `cargo update`
2) Verify:
   - `cargo fmt -- --check`
   - `cargo clippy --all-targets --all-features -- -D warnings`
   - `cargo test`
3) If failures occur, revert `Cargo.lock` and open a PR explaining which crate update broke and why.

```




